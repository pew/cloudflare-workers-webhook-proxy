{
  "name": "cloudflare-workers-webhook-proxy",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.18.0",
    "prettier": "^2.7.1",
    "typescript": "^4.8.4",
    "wrangler": "2.20.1"
  },
  "private": true,
  "scripts": {
    "format": "prettier --write  '*.{json,js}' 'src/**/*.{js,ts}'",
    "lint": "eslint --fix .",
    "start": "wrangler dev",
    "deploy": "wrangler publish"
  }
}
